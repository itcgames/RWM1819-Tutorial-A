/* global diamond_achievement, describe, it, expect, should */

describe('diamond_achievement()', function () {
  'use strict';

  it('diamond achievment constructor exists', function () {
    expect(constructor).to.be.a('function');

  });

  // Add more assertions here
});
