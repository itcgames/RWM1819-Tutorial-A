/* global tutorial_text, describe, it, expect, should */

describe('tutorial_text()', function () {
  'use strict';

  it('tutorial constructor exists', function () {
    expect(constructor).to.be.a('function');

  });

  // Add more assertions here
});
