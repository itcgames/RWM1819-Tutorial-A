/* global highlight, describe, it, expect, should */

describe('highlight()', function () {
  'use strict';

  it('highlight constructor exists', function () {
    expect(constructor).to.be.a('function');

  });

  // Add more assertions here
});
