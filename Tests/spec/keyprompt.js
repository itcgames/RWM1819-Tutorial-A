/* global keyprompt, describe, it, expect, should */

describe('keyprompt()', function () {
  'use strict';

  it('keyprompt constructor exists', function () {
    expect(constructor).to.be.a('function');

  });

  // Add more assertions here
});
