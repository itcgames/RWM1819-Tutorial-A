/* global prompt, describe, it, expect, should */

describe('prompt()', function () {
  'use strict';

  it('prompt constructor exists', function () {
    expect(constructor).to.be.a('function');

  });

  // Add more assertions here
});
