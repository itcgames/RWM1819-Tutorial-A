/* global arrow, describe, it, expect, should */

describe('arrow()', function () {
  'use strict';

  it('arrow constructor exists', function () {
    expect(constructor).to.be.a('function');

  });

  // Add more assertions here
});
